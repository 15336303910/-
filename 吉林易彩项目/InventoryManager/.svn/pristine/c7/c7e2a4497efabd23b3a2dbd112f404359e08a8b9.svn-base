Ext.namespace("com.increase.cen.poleline");
 com.increase.cen.poleline.PolelineWindow = Ext.extend(Ext.Window,{
	//width:650,
	autoHeight:true,
	closeAction:'close',//当关闭按钮被点击时执行的操作
	closable:true,
	layout: 'fit',//布局
	bodyStyle:'padding:5px;',
    buttonAlign:'center',
	plain:true,//设置为true将把窗体的body区域用一个透明背景来渲染
	border:false,
	modal:true,//将使窗口成为模态的
	frame:true,
	initComponent:function(config){
		//重写initComponent superclass.initComponent.call(this);来调用父类的初始化方法.
		com.increase.cen.poleline.PolelineWindow.superclass.initComponent.call(this);  
	}
	
});
Ext.reg("polelineWindow",com.increase.cen.poleline.PolelineWindow );